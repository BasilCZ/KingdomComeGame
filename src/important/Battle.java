package important;

public class Battle {
    public static void battle(Player p, Entity e){
        boolean playerGiveUp = false;
        boolean entityGiveUp = false;
        String input;
        while(p.getHp() != 0 && e.getHp() != 0 && !playerGiveUp && !entityGiveUp){
            //Player's turn

            //Enemy's turn
        }
    }
}
